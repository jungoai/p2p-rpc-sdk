name:           "n1"
httpEndpoint:   "http://192.168.56.53:8001" # NOTE: It's mandatory e.g: 192.65.7.2:8000
                                            # NOTE: you should configure a reverse proxy
                                            # to map <httpEndpoint>/<chain-id> to
                                            # the running chain address
httpPort:       4001
p2pPort:        3101  # NOTE: You should specify it to be accessible as a bootstrapper or when using docker image
                      # otherwise it's optional and would be a random port that would be printed in output
isBootstrap:    true
bootstrappers:  []
